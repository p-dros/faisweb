const links = {
  root: '/',
  courses: '/courses',
  lecturers: '/lecturers',
  ranking: '/ranking',
  login: '/sign-in',
  register: '/sign-up',
  profile: '/profile',
} as const

export default links
